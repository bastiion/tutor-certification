/** Issued certificate JSON field `schema_version` (signed). */

export const CURRENT_CERT_SCHEMA_VERSION = 1;

/** Revocation POST body field `schema_version` (unsigned metadata). */

export const CURRENT_REVOCATION_SCHEMA_VERSION = 1;
