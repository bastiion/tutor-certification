/**
 * Shared browser-side cryptography helpers (libsodium, HKDF, etc.)
 * will live here. This package is a deliberate placeholder until those flows are implemented.
 */

export type CryptoPackageStatus = "not-implemented";

export function cryptoPackageStatus(): CryptoPackageStatus {
  return "not-implemented";
}
