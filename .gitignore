# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage (frontend or generic)
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

.direnv

# Nix
result

# Cypress (optional local artifacts)
cypress/screenshots
cypress/videos
cypress/downloads

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# PHP API (Composer, generated reports, tooling caches — keep composer.lock tracked)
api/vendor
api/coverage
api/public/static-spa

# PHPUnit 10+/Pest caches (often under api/ but match anywhere)
.phpunit.cache
.phpunit.result.cache

# PHPStan caches / temp (default result-cache path when running from api/)
api/tmp/phpstan
api/.phpstan.cache

# Composer local installs / env (do not commit secrets)
api/auth.json
api/.composer
composer.phar

# Local SQLite databases (PDO dev / future schema seeds)
*.sqlite
*.sqlite-journal
*.sqlite3
